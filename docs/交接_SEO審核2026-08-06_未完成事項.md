# SEO/AEO/GEO 審核（2026-08-06）：未完成事項交接

日期：2026-08-06
狀態：**P0 主體已完成、已 push、已上線驗證；剩返兩項需要 Mic 親自操作**
相關 commit：`b2df201 fix(seo/aeo/geo): 執行 2026-08-06 網站審核報告 P0 批次`
來源報告：`OneDrive\Desktop\DestinySolver_SEO_AEO_GEO_Audit_2026-08-06.md`

---

## 一、已完成並上線驗證（唔使再做）

用 curl 直接讀線上 HTML／sitemap／llms.txt 逐項驗證過，唔係推測：

- topic-27／34／37 三篇健康生死斷語文章：`<meta name="robots" content="noindex, follow">` 已生效，FAQPage schema 已移除，健康命理分類統一加咗醫療免責聲明橫額（`HealthDisclaimer.tsx`）
- topic-14→07、topic-18→08、topic-36→30 三組重疊教學文合併，內容已整合入目標文章，舊 slug 308 永久轉址已生效（Next.js `permanent: true` 出嚟係 308 唔係 301，對 Google 等價，唔使理）
- topic-25 AI 對話殘留同重複內容區塊已清走，死亡斷語已軟化；topic-02／04 生成提示殘留標題已清走
- 新增 `/privacy`、`/terms`、`/disclaimer` 三頁，200 狀態、footer 已連結、已入 sitemap
- llms.txt／llms-full.txt 已同步剔走上述 noindex／已刪除文章
- 首頁動畫（`InkFlowHero.tsx`、`HomeMotion.tsx`）加咗 `max-width: 767px` 判斷，手機版跳過 GSAP 進場動畫、內容即時以最終狀態顯示；桌面版動效不變。已確認部署咗嘅 JS chunk 入面含呢個判斷式
- sitemap／robots／llms.txt 數字對得上：topic 35篇−3 noindex=32、post 95篇−7篇未來日期=88（8/7～8/10 排隊發布）

副作用（唔使處理，會自動復原）：健康命理分類頁暫時跌出 sitemap，因為淨低文章全部 noindex 或未來日期；8/10 `post-20260810-01` 一發布就會自動返返入去。

## 二、未完成，需要 Mic 親自操作嘅兩項

### 1. 手機效能實測（P0-4）— 2026-08-06 續做完成，仍未達標，需再一輪

Mic 親自用 pagespeed.web.dev 測咗（前一輪本機/API 429 兩度打唔通）。實測結果：**效能 55／100，LCP 13.8s／FCP 12.8s／Speed Index 12.8s／TBT 0ms／CLS 0**。同基線（14.2s／12.8s）比，動畫修正（8/6 首輪）幫助極微，證實樽頸唔喺動畫。

真正病因查到：首頁三個 CSS chunk（88.7KiB）入面塞咗 **536 條 `@font-face`**（Noto Sans TC 211 + Noto Serif TC 325），呢個唔係 `subsets` 參數可以收窄——實測 `subsets: ['chinese-traditional']` 喺 next/font 直接 TypeScript 報錯非法值（Noto TC 呢類 next/font 字型淨係認 cyrillic/latin/latin-ext/vietnamese，CJK 字符集係字體本身內建、唔受 subsets 篩），即係話呢 88.7KiB 嘅 CSS 大小基本上係「有齊繁中字」嘅硬成本，冇平白減嘅巷。已做咗嘅兩項跟進（commit `1c5c033`）：
- `preload: false` 加落兩個字型：本來 next/font 預設會將 20 幾個 woff2 全部 `<link rel=preload>` 搶頭批頻寬同真正關鍵資源爭；而家淨靠 `display:swap` 背景載入，減低網絡爭用。
- header「DESTINY SOLVER」＋兩個 `▾` 箭嘴 `#8A8071→#6B6155`：PSI 無障礙稽核揪到嘅對比度漏網之魚（P3-8 嗰次淨改咗 footer，冇覆蓋 header 呢幾處）。

冇動嘅：font-serif 三級字重（400/700/900）喺 16 個檔案都有實際用到（黑體標題＋粗體＋計算機細字），冇冗餘可以砍，強行砍會出現合成粗體/走樣風險。

**續做**：呢輪修正未必夠推 LCP 落 2.5 秒（CSS 大小基本冇變，只係唔再搶頻寬），要再測一次先知實際效果。續做步驟同上（pagespeed.web.dev 測 Mobile）。若仲遠超 2.5 秒，下一輪要諗嘅係徹底跳出 next/font 自動切片，改用手動預先 subset 過嘅單一 woff2（用 `pyftsubset`／`glyphhanger` 呢類工具，掃晒全站實際用到嘅繁中字砌一隻幾百 KB 嘅精簡字型），呢個係較大嘅架構決定（要掃全部文章內容抽字表、有漏字風險），做之前應該同 Mic 講清楚 tradeoff 先落手，唔好自己靜雞雞試。

### 2. Search Console／Bing Webmaster 網域驗證（P0-1）

呢步一定要 Mic 登入自己嘅 Google／Microsoft 帳戶先做到，Claude 冚唪唥做唔到（涉及帳戶設定變更，屬於要 Mic 明確授權嘅動作類別）。

步驟（照報告 P0-1 原文）：
1. Google Search Console 用 Domain property 驗證 `destinysolver.com`
2. 提交 `https://www.destinysolver.com/sitemap.xml`
3. URL Inspection 查首頁、最新文章、分類、詞彙頁：睇 Discovery／Crawl／Indexing／Canonical／Rendered HTML，特別留意 Google-selected canonical 係咪等於頁面自己個 self-canonical
4. Bing Webmaster Tools 同樣驗證＋提交 sitemap
5. 匯出 Page indexing 報告，搵 Discovered currently not indexed／Crawled currently not indexed／Duplicate without user-selected canonical 呢幾類問題

## 三、其餘 P1／P2 事項（報告有寫，今次未動）

刻意冇喺呢一輪處理，屬於下一輪範圍，唔係漏做：

- P0-3 全 38 篇 topic-* 中，剩低 30 篇「Keep」冇動過（合併咗 3、改咗 3、noindex 咗 3）；其餘全部人工睇過評為乾淨，唔使再查
- P1-1／P1-2／P1-3／P1-4／P1-5：sitemap 資料品質、部署後自動驗證、內容架構收斂、AEO 模板、原創證據強化
- P2-1～P2-4：llms.txt 精簡、第三方權威訊號、IndexNow、量度框架

## 四、Chrome 連線已知問題（同呢輪 SEO 工作無關，記低避免下次重複排查）

呢個 session 全程 `claude-in-chrome` MCP 連唔到 extension，重複試七次以上、重開 Chrome 都冇用，最終判斷係 MCP 連線喺 Claude Code process 啟動嗰刻已經綁定，中途換帳號／重開瀏覽器都唔會令佢重新連線。**只有完全退出成個 Claude Code CLI process、開新 terminal 重新 `claude` 先會重新綁定。** 本輪所有線上驗證改用 `curl` 直接讀 HTML／sitemap／llms.txt 完成，唔靠 Chrome。
