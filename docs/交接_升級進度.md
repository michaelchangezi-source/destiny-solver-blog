# destinysolver 升級計劃執行進度（2026-07-31 收工）

規格書：`C:\Users\micha\OneDrive\Desktop\destinysolver-upgrade-plan.md`
下一個 session 開工前必讀嗰份規格書的 §7（驗收向量）同 §8（護欄）。

## Git 狀態

| 分支 | 內容 | 可否部署 |
|---|---|---|
| `main` | 只有 P0 修錯與測試框架，全綠 | 可以，安全 |
| `wip/p1-upgrade` | P1 至 P3 大部分成果，UI 未完 | 不可以，tsc 有 3 個 error |

Vercel 追 `main`，所以未完成的嘢刻意冇 merge 入去。續做時 `git checkout wip/p1-upgrade`。

## 已完成並有證據

### M1 P0 修錯（已在 main，已驗收）
- P0-1 `bazi-calc.ts` 子的藏干由壬改癸，同步改咗 `destiny.solver\bazi-chart.html`
- P0-2 `bazi-compat.ts` 三合局木與金對調修正
- P0-3 同支相遇改判伏吟，不再誤標半三合
- P0-4 半三合加旺神校驗，缺旺神改標拱合
- 驗收：`npm test` 新增回歸測試，做過負向對照（種返 bug 會 fail）

### 在 wip 分支，已跑過測試
- P1-1 真太陽時：經度時差＋均時差，含校正前後時柱對照資料
- P1-2 分鐘輸入與子時換日：`calculate()` 加 optional options，舊簽名冇變
- P1-3 十二長生、神煞、空亡、納音：新檔 `src\lib\bazi-shensha.ts`
- P1-4 時間軸資料層：新檔 `src\lib\bazi-timeline.ts`
- P1-8 合盤深化：三刑自刑、文案分層、雙向視角、readingTags
- P1-7 前置：`src\lib\glossary.ts` 由 12 條擴至 53 條，162 個 relatedSlugs 全部驗過真實存在
- P2-1 定價三層 800／1,600／2,400 上首頁，Offer schema 同步改成三個
- P2-2 自我篩選雙欄上首頁
- P2-3 三條承諾
- P3-1／3-2／3-3 見證同意聲明、about 敘事重組、服務說明版位

測試現況：`node tests\bazi-spec.mjs` 141 項全過，`node tests\compat-spec.mjs` 30 項全過。

## 未完成，下次由呢度開始

### 1. 最優先：BaziCalculator.tsx UI 接線
`npx tsc --noEmit` 現有 3 個 error，全部喺 `src\app\bazi\BaziCalculator.tsx`：
- 418 行 `hourBranchOf` 未 import
- 679 行 PillarCard 呼叫缺 `detail`、`dayStem`、`kong` 三個 prop
- 696 行 DaYun 卡呼叫缺 `isSelected`、`tenGod`、`stage`、`onSelect` 四個 prop

引擎層全部做完並測試過，呢度只係將資料接上介面。做完必須跑齊：`npm test`、`npx tsc --noEmit`、`npm run lint`（原有 2 個 pre-existing error 唔關事，唔准增加）。

### 2. 未開始
- P1-5 命盤庫：站主拍板用 localStorage，唔開資料庫
- P1-6 匯出 PNG／PDF、PWA、打印樣式
- P1-7 接線：工具上的術語連去詞彙表、命盤結果推薦文章（詞彙表資料已備好）
- P5 效能瘦身：目標總傳輸低於 800KB、Lighthouse 85 分以上

### 3. 收工前一定要做
開 preview 實跑 `/bazi` 同 `/compat` 驗一次，唔可以只靠測試綠燈就當完成。

## 站主已拍板的決定，唔好再問

1. 定價三層 HK$800 ／ HK$1,600 ／ HK$2,400，中間層標「最多人選」。舊客回購價唔加。
2. 語域維持繁體書面語，轉化頁都唔轉廣東話口語。
3. AI 撰文聲明唔加，只做三條承諾。
4. 命盤庫用本機儲存。
5. WhatsApp 直達整項唔做，唔加設定欄位。

## 等站主補資料的 TODO

- `src\components\home\PricingTiers.tsx` 開頭註釋：1,600 同 2,400 兩層的方案名稱與交付物清單、三層的付款時點與方式、諮詢時長。未填之前兩張卡只顯示價格。
- `src\app\consultation\page.tsx` serviceDetails：交付物、時長、付款時點、改期政策、可否追問，五項留空，版面自動唔顯示。
- `src\app\consultation\page.tsx` 見證區：截圖版位等站主提供已取得同意並打碼的截圖。現有四則文字見證係真實素材，一個字都冇改過。
